# 班级管家 · Class Assistant — 项目目标书

> 版本：v0.1（草案，待老师确认）
> 日期：2026-09-08
> 状态：规划阶段，代码未开始
> 关联项目：`E:\OpenCode\review-helper`（复习伴侣，降级为算法参考库）

---

## 一、项目背景与定位

由老师提议发起：在现有「复习伴侣」（学生个人复习工具）基础上，扩展为**完整的班级管理系统**，以微信小程序为最终形态。

**一句话定位**：面向单个班级（≤100 人）的微信小程序，覆盖「通知资料分发 → 信息收集统计 → 成绩管理分析 → 复习练习」四类场景；**AI（LLM）是贯穿各模块的增强引擎，但整体可开关**——AI 关闭时，全部基础功能照常工作。

**三个设计原则**：

1. **AI 可选**：所有 LLM 能力均为可选增强，带全局开关 + 双通道切换；基础功能零 LLM 依赖
2. **零运维**：基于微信云开发，不买服务器、不管理备案域名
3. **合规优先**：内容安全检测全覆盖，成绩等敏感信息按最小可见原则设计

---

## 二、目标用户与角色

| 角色 | 谁来用 | 能做什么 |
|------|--------|----------|
| 超级管理员 | 开发者/老师 | 全部权限 + 邀请码发放 + AI 配置 + 反馈/日志查看 |
| 管理员（老师/班委） | 老师及指定的班委 | 发布通知/资料、录成绩、发起收集表、发题库 |
| 普通成员（学生） | 班级同学（凭姓名+学号认证） | 查看通知/资料、提交收集、查看个人成绩与排名、练习复习 |

主体说明：开发与试用阶段用个人主体小程序（体验版免审，二维码邀请）；正式发布时挂靠老师提供的主体，届时可解锁合规的正式类目。

---

## 三、功能目标：分四个里程碑交付

### M1 班级通行证（认证 + 权限 + 通知/资料分发 + AI 通知草稿）

| 功能 | 类型 | 说明 |
|------|------|------|
| 成员认证 | 核心 | 姓名+学号比对 `class_members` 名单；名单由管理员导入 |
| 三级权限 + 邀请码 | 核心 | 成员/管理员/超级管理员；一次性邀请码开通管理权限 |
| 通知发布 | 核心 | 分类（考试安排/作业信息/活动信息/班级通知/其他）、置顶、编辑、删除 |
| 资料分发 | 核心 | 图片、附件（云存储，大小/类型限制）、外链；**替代聊天记录翻找** |
| 收藏 + 订阅消息提醒 | 核心 | 新通知订阅提醒（微信订阅消息） |
| 内容安全检测 | 核心 | 发布前 `msgSecCheck` v2，输入与 AI 输出双侧校验 |
| 操作日志 | 核心 | 发布/编辑/删除/授权全量审计（脱敏） |
| **AI 通知草稿** | AI 增强 | 管理员一句话 → 结构化草稿（标题/分类/时间/地点/正文自动提取），确认后发布 |

**M1 验收标准**：老师与 5 名同学真实使用——认证、发布含附件的通知、收藏、收到订阅提醒全流程跑通；AI 草稿生成成功率 ≥ 90%（失败可手动发布兜底）。

### M2 成绩中心

| 功能 | 类型 | 说明 |
|------|------|------|
| 考试/科目管理 | 核心 | 按考试（如「9 月月考」）+ 科目组织成绩 |
| 成绩录入 | 核心 | 手动单条 + **Excel 批量导入**（云函数解析，模板可下载） |
| 统计分析 | 核心 | 均分/中位数/最高最低/标准差、分数段分布直方图、及格率优秀率、历次考试趋势（ECharts 小程序版） |
| 个人定位 | 核心 | 学生端**默认只看自己的**单科成绩与班级位置（按四分位/百分比呈现，不做公开排名榜） |
| 数据导出 | 核心 | 管理员导出 Excel |
| **AI 班级分析报告** | AI 增强 | LLM 结合统计结果生成班级分析（失分模式、进步/退步学生名单、教学建议） |
| **AI 个人评语** | AI 增强 | 按学生批量生成个性化评语（家长会/期末评语直接可用），人工确认后导出 |

**M2 验收标准**：真实一次考试的完整成绩通过 Excel 导入；管理员 3 分钟内拿到统计图表 + AI 分析报告；学生可在手机上看到自己的成绩与定位。

### M3 信息收集与统计

| 功能 | 类型 | 说明 |
|------|------|------|
| 收集表/接龙/报名 | 核心 | 题型：单选/多选/文本；截止时间；已交/未交名单 |
| 结果统计可视化 | 核心 | 选项占比、文本回答列表、导出 |
| **AI 归类汇总** | AI 增强 | 开放性文本回答（如「对班会的建议」）→ LLM 自动归类 + 摘要成结论 |

**M3 验收标准**：一次真实报名或问卷（≥30 人提交）从发起到出统计结果全流程 ≤ 10 分钟。

### M4 学习模块（复用 review-helper 资产）

| 功能 | 类型 | 说明 |
|------|------|------|
| 班级题库 | 核心 | 管理员导入题库 JSON（沿用 review-helper 的题库 schema）；题库绑定考试/科目 |
| 练习 | 核心 | 选择/填空/判断三题型，做题、对答案、错题本 |
| 间隔复习 | 核心 | SM-2 调度（review-helper 已验证的逻辑移植：1/6/15/38 天梯度），今日到期卡片 |
| **AI 出题** | AI 增强 | 管理员粘贴知识点文本 → LLM 按题型生成题目 → 人工审核入库；AI 不可用时用规则引擎兜底（review-helper 规则引擎裁剪移植） |

**M4 验收标准**：一次月考前，管理员导入 50 题，≥10 名学生完成练习且复习调度正确运转；错题本与掌握度统计可用。

---

## 四、明确不做的（交付边界）

- ❌ 多班级 / 学校级组织架构 / SaaS 化（单班级工具）
- ❌ 家长端独立身份（学生端设计已覆盖家长查看场景；家长需求后续议）
- ❌ 实时聊天、朋友圈类社区（微信群已覆盖）
- ❌ 支付、会员、广告
- ❌ 小程序端文档解析（PDF/Word/OCR——小程序无 DOM，pdf.js 跑不了；M4 出题素材以粘贴文本/题库文件为主）
- ❌ 原生 iOS/Android APP（PWA/APK 由 review-helper 主线承担，互不挤占）

---

## 五、技术方案

### 5.1 架构

```
微信小程序（原生开发，无第三方框架）
    │
    ├── 云数据库（约 20 个集合，继承 Class-Box 设计并扩展成绩/收集表/题库）
    ├── 云存储（资料附件、成绩 Excel 模板与导入文件）
    └── 云函数（业务逻辑 + LLM 统一网关）
              │
              ├── 通道 A：云开发内置 AI（@cloudbase/node-sdk ai() 网关，托管计费）
              └── 通道 B：OpenAI 兼容 API（环境变量 Key：GLM-4-Flash 免费 / DeepSeek / 通义）
                  ※ 管理端可配：AI 开关、通道选择、模型名；默认关闭，逐功能开启
```

- 通道 A 优点：不管 Key、不出网流量、与云开发打通（Class-Box 已验证）
- 通道 B 优点：模型可选、成本可控、复用 review-helper 的 BYOK 经验
- LLM 输出统一走「严格 JSON 校验 + 字段白名单清洗 + warnings 机制 + 审计日志」（复刻 Class-Box `parseNoticeWithAI` 的 sanitizeDraft 模式）

### 5.2 数据库集合（M1 部分，继承 Class-Box）

`users`（身份/角色）、`class_members`（名单）、`admin_invite_codes`（邀请码）、`notices`（通知+附件+链接）、`subscribers`（订阅）、`favorites`（收藏）、`feedbacks`（反馈）、`security_counters`（事务性限流）、`operation_logs`、`ai_usage_logs`

M2 追加：`exams`（考试）、`subjects`、`scores`（成绩，学号关联，不存多余个人信息）
M3 追加：`surveys`、`survey_items`、`survey_submissions`
M4 追加：`question_banks`、`questions`（对齐 review-helper 题库 schema）、`review_cards`（SM-2 状态）、`mistakes`（错题本）

### 5.3 工程目录（预设）

```
class-assistant/
├── miniprogram/          # 小程序端
├── cloudfunctions/       # 云函数（含 ai-gateway 统一网关）
├── docs/                 # database.md / deploy.md / security.md（沿 Class-Box 文档规范）
├── scripts/              # 本地辅助脚本（Excel 模板生成、名单导入等，Python · conda opencode 环境）
├── GOALS.md              # 本文档
└── PLAN.md               # 执行计划（目标书确认后编写）
```

---

## 六、从 Class-Box 取用的资产清单

仓库已核查：**非空仓库**，118 个文件，MIT 协议，完整可运行。取「设计」，不直接搬代码：

| # | 资产 | 用法 |
|---|------|------|
| 1 | 14 个集合的数据库设计文档（含索引建议、安全边界） | 直接继承，扩展新集合 |
| 2 | 权限模型（三级角色 + 一次性邀请码 + 名单认证流程） | 逻辑复刻 |
| 3 | 内容安全接入模式（msgSecCheck v2、87014 处理、双侧校验） | 直接照搬模式 |
| 4 | 事务性限流（固定时间桶 + runTransaction） | 直接照搬模式 |
| 5 | AI 草稿云函数全流程（prompt 约束/sanitize/warnings/错误分类/审计） | 作为 ai-gateway 的标准模板 |
| 6 | 订阅消息收发（saveNoticeSubscriber / sendNoticeMessage） | 逻辑复刻 |
| 7 | 文档规范（database/deploy/security 三件套写法） | 沿用 |

---

## 七、成本预算

| 项 | 开发/试用期 | 正式上线后 |
|----|-------------|------------|
| 云开发 | 0 元（免费云环境，未发布状态持续免费） | 19.9 元/月（基础套餐：20 万次调用/月、2GB 存储，单班级余量极大） |
| LLM | 通道 B 免费模型（GLM-4-Flash）起步 ≈ 0 | 通道 A/B 任选，预估 5~20 元/月（老师提供 AI 费用后可上更好的模型） |
| 小程序认证 | 0（个人主体） | 视主体情况（部分主体类型免认证费） |
| **合计** | **≈ 0 元** | **≈ 25~40 元/月** |

风险提示：免费云环境活动截止 2026-12-31，正式上线前需确认主体与付费方案衔接。

---

## 八、合规与风险

| # | 风险 | 等级 | 对策 |
|---|------|------|------|
| 1 | 个人主体无法用教育类目 | 高（已确认平台规则） | 试用期内用体验版免审分发；正式发布挂老师主体，走「工具-效率」或学校资质类目 |
| 2 | 成绩属未成年人敏感信息 | 中 | 最小可见（学生只看自己）、学号关联不展示全名给无关方、隐私协议、管理员导出留审计 |
| 3 | UGC 内容违规 | 中 | 全部发布路径过 msgSecCheck；AI 输出同样过检后才返回 |
| 4 | LLM 输出格式/事实错误 | 中 | JSON 严格校验 + 白名单清洗 + warnings；AI 生成内容一律「人工确认后生效」 |
| 5 | 免费云环境政策到期 | 低 | 上线前转付费套餐（19.9/月在预算内） |
| 6 | 订阅消息一次性授权限制 | 低 | 每次提醒消耗一次授权，发通知时引导用户勾选「总是保持以上选择」 |

---

## 九、开发环境约定

- 本地 Python 辅助脚本（Excel 处理、名单/题库转换等）**统一使用 conda `opencode` 环境，禁止使用 base**
- 小程序/云函数开发调试在微信开发者工具内进行（Node.js 版本跟随云函数运行时）
- 云函数密钥一律放云函数环境变量，不进前端代码、不进 git

---

## 十、总目标一句话

**四个月左右，交付一个老师真实愿意用、同学打开率高的班级小程序：M1 可试用 → M2 成绩分析进课堂 → M3 班务自动化 → M4 接上复习伴侣的学习闭环；AI 能力随老师预算逐步点亮，但任何时候拔掉 AI，系统依然完整可用。**
